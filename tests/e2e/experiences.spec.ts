import { test } from '@playwright/test'

test.describe('Experiences page (/experiences)', () => {
  test.fixme('renders heroHeadline', async ({ page }) => {})
  test.fixme('renders discount badge text', async ({ page }) => {})
  test.fixme('renders at least one experience card', async ({ page }) => {})
  test.fixme('renders property filter chips', async ({ page }) => {})
  test.fixme('renders bottom CTA', async ({ page }) => {})
})
