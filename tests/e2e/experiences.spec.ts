import { test } from '@playwright/test'

test.describe('Experiences page (/experiences)', () => {
  test.fixme('renders heroHeadline', async () => {})
  test.fixme('renders discount badge text', async () => {})
  test.fixme('renders at least one experience card', async () => {})
  test.fixme('renders property filter chips', async () => {})
  test.fixme('renders bottom CTA', async () => {})
})
