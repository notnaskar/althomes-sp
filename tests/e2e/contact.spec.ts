import { test } from '@playwright/test'

test.describe('Contact page (/contact)', () => {
  test.fixme('renders hero section', async () => {})
  test.fixme('renders phone number as tel: link', async () => {})
  test.fixme('renders email as mailto: link', async () => {})
  test.fixme('renders office address', async () => {})
  test.fixme('renders social links (Facebook and Instagram) from site settings', async () => {})
  test.fixme('form has Name input', async () => {})
  test.fixme('form has Email input', async () => {})
  test.fixme('form has Phone input', async () => {})
  test.fixme('form has Message textarea', async () => {})
  test.fixme('form has privacy consent checkbox', async () => {})
  test.fixme('submitting empty form shows required field errors', async () => {})
  test.fixme('submitting valid form shows success state', async () => {})
})
