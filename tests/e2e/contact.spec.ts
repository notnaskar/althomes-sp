import { test } from '@playwright/test'

test.describe('Contact page (/contact)', () => {
  test.fixme('renders hero section', async ({ page }) => {})
  test.fixme('renders phone number as tel: link', async ({ page }) => {})
  test.fixme('renders email as mailto: link', async ({ page }) => {})
  test.fixme('renders office address', async ({ page }) => {})
  test.fixme('renders social links (Facebook and Instagram) from site settings', async ({ page }) => {})
  test.fixme('form has Name input', async ({ page }) => {})
  test.fixme('form has Email input', async ({ page }) => {})
  test.fixme('form has Phone input', async ({ page }) => {})
  test.fixme('form has Message textarea', async ({ page }) => {})
  test.fixme('form has privacy consent checkbox', async ({ page }) => {})
  test.fixme('submitting empty form shows required field errors', async ({ page }) => {})
  test.fixme('submitting valid form shows success state', async ({ page }) => {})
})
