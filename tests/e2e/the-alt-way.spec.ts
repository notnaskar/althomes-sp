import { test } from '@playwright/test'

test.describe('The Alt Way page (/the-alt-way)', () => {
  test.fixme('renders heroHeadline', async () => {})
  test.fixme('renders mission section with missionText', async () => {})
  test.fixme('renders value props section with at least one item', async () => {})
  test.fixme('renders promise CTA section', async () => {})
  test.fixme('renders stats bar with at least one stat', async () => {})
  test.fixme('renders reviews carousel', async () => {})
  test.fixme('renders bottom CTA', async () => {})
})
