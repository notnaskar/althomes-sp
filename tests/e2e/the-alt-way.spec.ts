import { test } from '@playwright/test'

test.describe('The Alt Way page (/the-alt-way)', () => {
  test.fixme('renders heroHeadline', async ({ page }) => {})
  test.fixme('renders mission section with missionText', async ({ page }) => {})
  test.fixme('renders value props section with at least one item', async ({ page }) => {})
  test.fixme('renders promise CTA section', async ({ page }) => {})
  test.fixme('renders stats bar with at least one stat', async ({ page }) => {})
  test.fixme('renders reviews carousel', async ({ page }) => {})
  test.fixme('renders bottom CTA', async ({ page }) => {})
})
