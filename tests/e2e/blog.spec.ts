import { test } from '@playwright/test'

test.describe('Blog index page (/blog)', () => {
  test.todo('renders post cards grid with at least one card')
  test.todo('no raw <img> tags — all images use next/image')
})
