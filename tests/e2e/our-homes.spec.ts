import { test } from '@playwright/test'

test.describe('Our Homes page (/our-homes)', () => {
  test.fixme('renders heroHeadline', async () => {})
  test.fixme('renders at least one property card', async () => {})
  test.fixme('availability search form has checkIn input', async () => {})
  test.fixme('availability search form has checkOut input', async () => {})
  test.fixme('availability search form has guests input', async () => {})
  test.fixme('submitting availability form with no dates shows validation error', async () => {})
})
