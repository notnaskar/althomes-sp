import { test } from '@playwright/test'

test.describe('Property detail page (/our-homes/[slug])', () => {
  test.fixme('renders hero image', async ({ page }) => {})
  test.fixme('renders property title', async ({ page }) => {})
  test.fixme('renders gallery section', async ({ page }) => {})
  test.fixme('renders specs strip (guests, bedrooms, bathrooms)', async ({ page }) => {})
  test.fixme('renders location section', async ({ page }) => {})
  test.fixme('renders highlights section', async ({ page }) => {})
  test.fixme('renders experiences section when experiences are linked', async ({ page }) => {})
  test.fixme('renders amenities section', async ({ page }) => {})
  test.fixme('renders causes section when content is present', async ({ page }) => {})
  test.fixme('renders reviews section when published reviews exist', async ({ page }) => {})
  test.fixme('renders bottom CTA with FIND AVAILABILITY button', async ({ page }) => {})
})
