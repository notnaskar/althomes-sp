import { test } from '@playwright/test'

test.describe('Join Us page (/join-us)', () => {
  test.fixme('renders heroHeadline', async ({ page }) => {})
  test.fixme('renders pullQuote text', async ({ page }) => {})
  test.fixme('renders bullet points list', async ({ page }) => {})
  test.fixme('renders body paragraph text', async ({ page }) => {})
  test.fixme('form has NAME input', async ({ page }) => {})
  test.fixme('form has EMAIL input', async ({ page }) => {})
  test.fixme('form has PHONE NUMBER input', async ({ page }) => {})
  test.fixme('form has LOCATION input', async ({ page }) => {})
  test.fixme('form has TYPE OF PROPERTY input', async ({ page }) => {})
  test.fixme('form has STATUS input', async ({ page }) => {})
  test.fixme('form has OPERATIONAL input', async ({ page }) => {})
  test.fixme('form has PHOTOS / WEBSITE LINK input', async ({ page }) => {})
  test.fixme('form has privacy consent checkbox', async ({ page }) => {})
  test.fixme('submitting empty form shows required field errors', async ({ page }) => {})
  test.fixme('submitting valid form shows success state', async ({ page }) => {})
})
