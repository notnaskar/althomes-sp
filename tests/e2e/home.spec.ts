import { test } from '@playwright/test'

test.describe('Home page (/)', () => {
  test.fixme('renders heroHeadline text', async () => {})
  test.fixme('renders 6 navLabel buttons with correct links', async () => {})
  test.fixme('nav links navigate to correct pages', async () => {})
})
